# Leave these blank to run in Demo / offline mode (no live model calls).
HY3_API_KEY=
HY3_BASE_URL=
HY3_MODEL=hy3

# Optional: FastAPI backend host/port used by the dev launcher.
# NR_BACKEND_HOST=127.0.0.1
# NR_BACKEND_PORT=8000
